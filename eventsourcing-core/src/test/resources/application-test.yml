edison:
  s3-properties:
    enabled: false
    bucketname: test
  ldap:
    enabled: false

  eventsourcing:
    snapshot:
      bucket-template: de-otto-{stream-name}-snapshots
service:
  encryption:
    password: test
    salt: test
aws:
  profile: ft3-nonlive

