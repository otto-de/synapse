edison:
  s3-properties:
    enabled: false
    bucketname: test
  ldap:
    enabled: false

synapse:
  aws:
    profile: ft3-nonlive
  snapshot:
    bucket-name: de-otto-promo-compaction-test-snapshots
  compaction:
    enabled: true

service:
  encryption:
    password: test
    salt: test

spring:
  main:
    allow-bean-definition-overriding: true
